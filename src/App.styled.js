import styled from "styled-components";

const APP_BACKGROUND_COLOR = "#F5F5FF";

const AppStyled = styled.div`
  background-color: ${APP_BACKGROUND_COLOR};
  min-height: 100vh;
`;

export default AppStyled;
